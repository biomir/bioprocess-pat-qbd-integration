# Control-strategy integration

The reference combines three evidence streams: direct CPP/design-space checks, multivariate PCA T²/SPE monitoring, and a PLS soft sensor. The final states are `nominal`, `review`, and `action`.

These are advisory states. No closed-loop actuation is implemented.
