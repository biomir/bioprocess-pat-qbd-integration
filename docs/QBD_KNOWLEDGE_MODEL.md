# QbD knowledge model

The reference distinguishes CQAs, CPPs, PAT variables, models/statistics, and control-strategy advisories. A variable can be useful for state estimation without itself being a CPP.

Traceability pattern:

`CQA -> risk -> CPP/process variable -> PAT measurement -> model -> criterion -> control -> verification`
