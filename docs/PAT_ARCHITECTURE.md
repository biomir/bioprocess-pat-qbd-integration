# PAT architecture

A production PAT stack should make timestamp, unit, source/instrument identity, calibration state, missingness, stale values, plausibility, and analyzer status explicit before modeling.

The reference includes PCA monitoring and a PLS soft sensor. A production model lifecycle would additionally control training-data provenance, versioning, applicability domain, recalibration criteria, drift monitoring, verification, and change control.
